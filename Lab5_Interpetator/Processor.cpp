#include "Processor.h"

void Processor::processLines(vector<string> lines) {
	/*TreeCreator creator;
	for (int i = 0; i < lines.size(); i++) {
		string currLine = lines[i];
		if (currLine.find("if") != string::npos) {
			while (lines[i + 1].find("endif") == string::npos) {
				creator.parseLine()
			}
		}
	}*/

}
